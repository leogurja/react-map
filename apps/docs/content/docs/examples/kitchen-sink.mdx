---
title: Kitchen sink
description: Multi-select, dynamic colors, borders, and custom hints
---

<KitchenSinkDemo />

Combines:

- `MultiSelectMap` with several countries selected
- `fill` / `stroke` functions driven by sample HDI data
- `borderStyle="dash-dot"` and custom `strokeWidth`
- `HintComponent` showing population, HDI, and area

Source lives in `components/kitchen-sink-demo.tsx` in this docs app.
